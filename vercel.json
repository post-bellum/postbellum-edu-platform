{
  "buildCommand": "npm run typecheck && npm run lint && npm run build",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/smartemailing-sync?mode=push", "schedule": "0 * * * *" },
    { "path": "/api/cron/smartemailing-sync?mode=reconcile", "schedule": "0 3 * * *" }
  ]
}
